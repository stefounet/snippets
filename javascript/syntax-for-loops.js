/**
 * For loops in JavaScript
 *
 * I wish it was as straightforward as in Python. :(
 * https://developer.mozilla.org/en/JavaScript/Reference/Statements/for
 * https://developer.mozilla.org/en/JavaScript/Reference/Statements/for...in
 */

// Basic for loop
for (var index=0, limit=items.length; index<limit; index++) {
  // Do something with items[index]
}

// For … in loop
for (var name in items) {
  // Do something with items[name]
}
